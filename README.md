# Telegram Dating Bot 💕

Бот для знакомств в Telegram с функционалом анкет, лайков/дизлайков и оплаты через Telegram Stars.

## Возможности

- 📝 Создание анкеты с фото, видео и описанием
- 👀 Просмотр анкет других пользователей
- ❤️ Лайки и дизлайки
- 💑 Мэтчи (взаимные симпатии)
- ⭐ Покупка дополнительных просмотров за Telegram Stars
- 🔄 Сброс лимита просмотров

## Установка

1. Клонируйте репозиторий
2. Создайте виртуальное окружение:
```bash
python -m venv venv
venv\Scripts\activate  # Windows
# или
source venv/bin/activate  # Linux/Mac
```

3. Установите зависимости:
```bash
pip install -r requirements.txt
```

4. Создайте файл `.env` на основе `.env.example`:
```bash
copy .env.example .env  # Windows
# или
cp .env.example .env  # Linux/Mac
```

5. Укажите токен бота в `.env` файле

## Запуск

```bash
python bot.py
```

## Структура проекта

```
telegram-dating-bot/
├── bot.py              # Главный файл запуска
├── config.py           # Конфигурация
├── requirements.txt    # Зависимости
├── database/
│   ├── __init__.py
│   └── models.py       # Модели БД
├── handlers/
│   ├── __init__.py
│   ├── profile.py      # Создание/редактирование анкет
│   ├── matching.py     # Просмотр анкет, лайки
│   └── payments.py     # Платежи через Stars
├── keyboards/
│   ├── __init__.py
│   └── keyboards.py    # Клавиатуры
├── utils/
│   └── __init__.py
└── media/              # Папка для медиа (не используется, файлы хранятся в Telegram)
```

## Настройка платежей

Бот использует Telegram Stars для платежей. Для работы платежей:
1. Создайте бота через @BotFather
2. Включите платежи в настройках бота
3. Telegram Stars работают автоматически без дополнительных провайдеров

## Лицензия

MIT
